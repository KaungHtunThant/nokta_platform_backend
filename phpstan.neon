includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 8
    paths:
        - app
        - database
        - routes
    # Enforce strong typing (no loose arrays / mixed across layers — see TECHNICAL-STRUCTURE).
    checkMissingIterableValueType: true
    treatPhpDocTypesAsCertain: true
